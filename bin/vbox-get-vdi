#!/bin/bash

## returns the path for the given VM

VM="$1"

INFO=`vboxmanage showvminfo "$VM" --machinereadable 2> /dev/null`
RET=$?

if [ $RET -ne 0 ]; then
    exit $RET
fi

echo "$INFO" | grep 'SATA Controller-0-0' | sed 's|".*"="\(.*\)"|\1|'
